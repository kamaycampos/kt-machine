# The video files live in a GitHub Release, never in the repository. A checkout
# on every scheduled run must stay small; run_once.py fetches only the one clip
# it is about to publish.
media/
*.pyc
__pycache__/
work/
